//*****************************************************************************
//
//! @file nemagfx_performance_test.h
//!
//! @brief Test the relationship of rotate performance of morton and tiling
//!        and burst length.
//
//*****************************************************************************
//*****************************************************************************
//
// ${copyright}
//
// This is part of revision ${version} of the AmbiqSuite Development Package.
//
//*****************************************************************************
#ifndef CONFIG_ROTATE_H
#define CONFIG_ROTATE_H

#include "nemagfx_performance_test.h"


const TestItem_t g_sTestItemList[] =
{
    //!<TestType,         TEX,      FB,      TexFormat,     FBFormat,     DCFormat,     texture/font, Src Burst size,    Des Burst size,    SamplingMode, SrcWidth,SrcHight,DesWidth,DesHight,32Angle,tiling,morton
    {TEST_UNKNOWN,      LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_NO_USED, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, TEST_NA,        466, 466, 466, 466, 0,     false,  false},
    //!< The first test was used to warm up the GPU, its result will not be print, leave it alone.
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,  false},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,  false},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,  false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,  false},

//another
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   false,   true},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   false,   true},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   false,   true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  false,  true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   false,   true},

//another
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   false},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   false},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  false},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   false},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   false},

//another
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGB24,    NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 89,   true,   true},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  466, 466, 466, 466, 89,   true,   true},


    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 90,   true,   true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_PS,  466, 466, 466, 466, 180,  true,  true},

    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   true},
    {TEST_ROTATE,       LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,   20, 250, 466, 466, 89,   true,   true},

};



#endif
