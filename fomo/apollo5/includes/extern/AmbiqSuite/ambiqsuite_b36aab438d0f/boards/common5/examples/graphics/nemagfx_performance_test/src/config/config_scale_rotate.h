//*****************************************************************************
//
//! @file config_scale_rotate.h
//!
//! @brief Test the relationship of scale_rotate performance of morton and tiling
//!        and burst length.
//*****************************************************************************
//*****************************************************************************
//
// ${copyright}
//
// This is part of revision ${version} of the AmbiqSuite Development Package.
//
//*****************************************************************************
#ifndef CONFIG_SCALE_ROTATE_H
#define CONFIG_SCALE_ROTATE_H

#include "nemagfx_performance_test.h"


//*****************************************************************************
//
// Test item list
//
//*****************************************************************************
const TestItem_t g_sTestItemList[] =
{
    //!<TestType,         TEX,      FB,      TexFormat,     FBFormat,     DCFormat,     texture/font, Src Burst size,    Des Burst size,    SamplingMode, SrcWidth,SrcHight,DesWidth,DesHight,32Angle,tiling,morton
    {TEST_UNKNOWN,      LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_NO_USED, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, TEST_NA,        466, 466, 466, 466, 0,     false,  false},
    //!< The first test was used to warm up the GPU, its result will not be print, leave it alone.

//Item 1
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  false},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, false,  true},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  false},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},


//Item 2

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  false},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, false,  true},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  false},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  false},

    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_32, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_16, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 180, 180, 57.3, true,  true},

//Item3
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_PSRAM, LT_SSRAM, NEMA_RGB565,   NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_SSRAM, LT_SSRAM, NEMA_RGBA8888, NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
    {TEST_SCALE_ROTATE, LT_SSRAM, LT_SSRAM, NEMA_RGB565,   NEMA_RGB24,    NEMADC_RGB24,   ST_TEXTURE, GPU_MSPI_BURST_SIZE_64, GPU_MSPI_BURST_SIZE_64, NEMA_FILTER_BL,  200, 200, 300, 300, 57.3, true,  true},
};



#endif
